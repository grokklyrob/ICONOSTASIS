{
  "name": "@iconostasis/helper",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local CORS/OSC helper — paired localhost egress (§9.5, §17)",
  "bin": {
    "iconostasis-helper": "./src/cli.mjs"
  },
  "scripts": {
    "start": "node src/cli.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
