{
  "name": "@iconostasis/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Graph + render + audio runtime — UI-free, headless-testable (§17)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "three": "^0.172.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/three": "^0.172.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
