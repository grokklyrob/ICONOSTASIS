{
  "name": "@iconostasis/m0-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Thin M0 demo shell — not part of packages/engine (§18)",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@iconostasis/engine": "workspace:*",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
